# Contributing

Create a short-lived `feature/`, `fix/`, `docs/`, or `chore/` branch and open a
pull request into `main`. Use Conventional Commits. Include production build,
Nginx proxy, container, accessibility, and core-compatibility impact.
