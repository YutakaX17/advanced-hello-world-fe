{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/YutakaX17/advanced-hello-world-fe/modules.schema.json",
  "title": "Frontend assembly manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "core", "modules"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "schemaVersion": {
      "const": 1
    },
    "core": {
      "$ref": "#/$defs/package"
    },
    "modules": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/package"
      }
    }
  },
  "$defs": {
    "package": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "package", "version", "repository", "ref", "style"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "package": {
          "type": "string",
          "pattern": "^(@[a-z0-9-]+/)?[a-z][a-z0-9-]*$"
        },
        "version": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
        },
        "repository": {
          "type": "string",
          "pattern": "^https://github\\.com/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+\\.git$"
        },
        "ref": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "factory": {
          "type": "string",
          "pattern": "^[A-Za-z_$][A-Za-z0-9_$]*$"
        },
        "style": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_.-]+\\.css$"
        }
      }
    }
  }
}
