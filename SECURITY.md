# Security

Use GitHub private vulnerability reporting. Never commit secrets. Keep the
runtime container unprivileged, apply browser security headers, and treat API
responses as untrusted data.

GitHub secret scanning and push protection are enabled. Rotate an exposed
credential before cleaning history or closing an alert.
