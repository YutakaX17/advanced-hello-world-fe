# Advanced Hello World Frontend

Vite-powered React and TypeScript assembler. It consumes the separately
versioned frontend core package and produces the deployable Nginx image.

```bash
npm install
npm run dev
```

During local development, `/api` is proxied to `http://localhost:8000`.
